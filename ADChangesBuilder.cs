using System;
using System.Collections.Generic;
using System.Text;

namespace DataImporter
{
    class ADChangesBuilder
    {
        private string _db_conn;

        public ADChangesBuilder(string db_conn)
        {
            _db_conn = db_conn;
        }

    }
}
