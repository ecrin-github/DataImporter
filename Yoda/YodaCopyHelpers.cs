using System;
using System.Collections.Generic;
using System.Text;
using PostgreSQLCopyHelper;

namespace DataImporter.Yoda
{
	public class YodaCopyHelpers
	{
		/*
		public PostgreSQLCopyHelper<Summary> study_list_copy_helper =
			new PostgreSQLCopyHelper<Summary>("pp", "study_list")
				.MapInteger("id", x => x.id)
				.MapVarchar("nct_number", x => x.nct_number)
				.MapVarchar("generic_name", x => x.generic_name)
				.MapVarchar("study_name", x => x.study_name)
				.MapVarchar("details_link", x => x.details_link)
				.MapVarchar("enrolment_num", x => x.enrolment_num)
				.MapVarchar("csr_link", x => x.csr_link);


		public PostgreSQLCopyHelper<SuppDoc> supp_doc_copy_helper =
			new PostgreSQLCopyHelper<SuppDoc>("sd", "supp_docs")
				.MapInteger("id", x => x.id)
				.MapVarchar("nct_number", x => x.nct_number)
				.MapVarchar("doc_name", x => x.doc_name)
				.MapVarchar("comment", x => x.comment)
				.MapVarchar("url", x => x.url);
				*/
	}
}
